import SwiftUI

/// Der eigene Laden: Profil, Leistungen, Team, Bilder, Einstellungen.
struct PartnerProfileScreen: View {
    var body: some View {
        PartnerPlaceholder(symbol: "person", title: "Profil")
    }
}
