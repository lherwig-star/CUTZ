import SwiftUI

/// Alle Buchungen des Ladens, nicht nur die von heute.
struct PartnerBookingsScreen: View {
    var body: some View {
        PartnerPlaceholder(symbol: "checkmark.square", title: "Buchungen")
    }
}
