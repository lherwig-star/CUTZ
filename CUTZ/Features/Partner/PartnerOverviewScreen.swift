import SwiftUI

/// Der Startbildschirm der Friseurseite: Was ist heute los?
///
/// Wird in einem der nächsten Schritte gebaut.
struct PartnerOverviewScreen: View {
    var body: some View {
        PartnerPlaceholder(symbol: "house", title: "Übersicht")
    }
}
