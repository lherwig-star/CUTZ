import SwiftUI

/// Die Woche auf einen Blick, plus Zeiten sperren.
struct PartnerCalendarScreen: View {
    var body: some View {
        PartnerPlaceholder(symbol: "calendar", title: "Kalender")
    }
}
