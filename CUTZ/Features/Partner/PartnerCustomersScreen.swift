import SwiftUI

/// Stammkunden — wer war wann da und was wurde gemacht.
struct PartnerCustomersScreen: View {
    var body: some View {
        PartnerPlaceholder(symbol: "person.2", title: "Kunden")
    }
}
